[build]
  publish = "test"
  # Deploy previews temporarily enabled for this PR (bigger UI change,
  # worth eyeballing before merge). Restored to skip-by-default right
  # after in a follow-up commit.
