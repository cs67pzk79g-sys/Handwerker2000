[build]
  publish = "test"
  # Deploy-preview skip TEMPORARILY DISABLED for this PR (big visual change,
  # user wants a live preview link). Restore the ignore rule after merging:
  # ignore = "if [ \"$CONTEXT\" = \"deploy-preview\" ]; then exit 0; else exit 1; fi"
